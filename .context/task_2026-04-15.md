# Task Checklist — 2026-04-15

## Summary
Three implementations: real blog view counter (Supabase), navbar active-state bug fix, and dynamic breadcrumb navigation.

---

### Backend
- [ ] Add `view_count` integer column (default 0) to `blog_posts` table
- [ ] Create Postgres RPC function `increment_blog_view(post_slug text)` for atomic increment
- [ ] Add RLS policy allowing anonymous `SELECT` and the RPC call on `blog_posts`
- [ ] Update `Blog` interface in `api.ts` to include `view_count`
- [ ] Update `mapSupabaseToBlog` / `getBlogs` to return `view_count`

### Frontend
- [ ] Remove fake `viewCount()` helper from `BlogPage.tsx`
- [ ] Replace all `viewCount(post)` calls with real `post.view_count` display
- [ ] Call `increment_blog_view` RPC on `BlogDetailPage.tsx` mount (once per session per slug)
- [ ] Fix Navbar `Math.max(0, ...)` → use raw `findIndex` result (allow `-1`)
- [ ] Create `Breadcrumb.tsx` component with conditional rendering (hidden on `/`)
- [ ] Integrate `Breadcrumb` into page layouts below Navbar
- [ ] Style breadcrumb to match the existing dark/crimson Tailwind design system

### Logic
- [ ] Deduplicate blog view increment (sessionStorage guard per slug)
- [ ] Ensure breadcrumb handles nested routes (`/blog/:slug`, `/services/:slug`, `/portfolio/:slug`)
- [ ] Verify `SpotlightNavContainer` correctly hides ambience when `activeIndex === -1`

---

### Verification
- [ ] `npm run build` passes with zero errors
- [ ] Browser test: blog view count increments on detail page visit
- [ ] Browser test: navbar has no active highlight on non-primary routes
- [ ] Browser test: breadcrumb appears on subpages, hidden on homepage
