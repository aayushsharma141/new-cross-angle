# R&D Discovery: Emil Kowalski (Motion Engineering)
- **Core Principle:** Animate by frequency of user encounter.
- **Key Extract:** Never scale from 0; always scale from 0.95. Replace `transition: all` with explicit GPU properties. Custom cubic-bezier easing curves.
- **Platform Application:** Mapped into Layer 2.1 tokens (`--ease-out`, `--ease-gallery`) and Layer 3.3 Frequency Matrix.
