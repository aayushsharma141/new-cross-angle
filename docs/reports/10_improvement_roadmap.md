# 10 Improvement Roadmap — CrossAngle Interior

**Objective:** Priority-based execution plan to reach the **Elite / FAANG-level** tier.

## Phase 1: Immediate Stability (High Impact, Low Effort)

| Task | Priority | Command |
|------|----------|---------|
| **Fix DB Columns** | 🔴 Critical | `ALTER TABLE public.testimonials ADD COLUMN created_at TIMESTAMPTZ DEFAULT NOW();` |
| **Restore Assets** | 🟠 High | Already implemented: Migrated to local `/noise.svg`. |
| **Hero Fallback** | 🟠 High | Add `poster="/hero-poster.webp"` and `<source type="video/webm">` to `Hero` component. |

## Phase 2: Performance Engineering (High Impact, High Effort)

- [ ] **LCP Optimization:** Use Vite-Plugin-Imagekit or similar to generate `srcset` automatically for portfolio grids.
- [ ] **Bundle Squashing:** Analyze `GSAP` and `Lucide` imports to ensure tree-shaking is active. 
- [ ] **Skeleton States:** Replace "Blank" loading screens with Shimmer/Skeleton loaders to improve perceived performance during Supabase calls.

## Phase 3: Automation & Growth (Medium Impact, Medium Effort)

- [ ] **Lead Webhooks:** Implement a Supabase Edge Function to push new leads to the owner's WhatsApp/Slack.
- [ ] **SEO Automation:** `/workflow generate_sitemap` — Automate `sitemap.xml` updates on every build.
- [ ] **Aggregate Schema:** Link `testimonials` rating directly to `LocalBusiness` JSON-LD to captured star ratings in Google SERP.

## Future Protocol Commands
Execute these in order to bridge the "Elite Gap":
1. `/fix testimonials_schema`
2. `/optimize hero_assets`
3. `/automate lead_notifications`

---
*Roadmap generated by Antigravity Elite Protocol.*
