/**
 * @deprecated Direct imports from this file still work, but prefer importing
 * from the bounded context modules under `@/services/media/` for new code.
 *
 * This shim preserves backward compatibility for all existing consumer files.
 */
export * from "./media/index";