/**
 * @deprecated Direct imports from this file still work, but prefer importing
 * from the bounded context modules under `@/lib/api/` for new code.
 *
 * This shim preserves backward compatibility for all 24 existing import sites.
 */
export * from "./api/index";