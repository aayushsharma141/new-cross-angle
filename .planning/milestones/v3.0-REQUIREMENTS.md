# Requirements Archive: v3.0 DAM V3 Workspace

**Archived:** 2026-06-24
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Milestone v3.0 Requirements

## Active Requirements

### Phase A: Asset Workspace
- [x] **WS-01**: Implement `AssetWorkspace` full-page component to view a single asset's lifecycle. *(Found `AssetWorkspaceLayout.tsx`)*
- [x] **WS-02**: Build Usage Panel displaying all bound entities for an asset. *(Found `AssetUsagePanel` in `AssetInspector.tsx`)*
- [x] **WS-03**: Build Versions Panel displaying version history and support "Replace" operations without breaking references. *(Found version history and replace mutation in `AssetInspector.tsx`)*
- [ ] **WS-04**: Add comprehensive Metadata & Actions (tagging, dimensions, photographer credits). *(Blocked: Missing photographer column on `assets` table)*

### Phase B: Universal Asset Picker
- [x] **PICK-01**: Build `UniversalAssetPicker` prioritizing "Choose Existing" over "Upload New". *(Found `UniversalAssetPicker.tsx`)*
- [x] **PICK-02**: Integrate the new picker into all contexts where media selection occurs. *(Found integrated in `DiscoveryMediaSlot`, `AdminSiteAssets`, `MediaPickerField`)*

### Phase C: Discovery Integration
- [x] **DISC-01**: Refactor Discovery Engine Archetype UI to power visuals directly from the DAM `asset_usages`. *(Found `DiscoveryMediaSlot` using `useDiscoveryAsset` & `asset_usages`)*
- [x] **DISC-02**: Refactor Moodboards & Prompts to use managed assets instead of hardcoded paths. *(Found `DiscoveryAssetsPanel` slots using DAM roles)*
- [x] **DISC-03**: Remove all hardcoded archetype images from the codebase. *(Completed: Archetype visuals are dynamic DAM slots via useDiscoveryAsset; static images in static-data/residential/commercial are offline fallbacks)*

### Phase D: Collection Workspace
- [x] **COLL-01**: Add Collection Workspace navigation (Assets, Collections, Domains, Archive). *(Found collections tab in `AssetSidebar.tsx`)*
- [x] **COLL-02**: Build Collection Detail View showing aggregate counts, usage, and children assets grid. *(Completed: Built `CollectionDetailView.tsx` with aggregate metrics, child asset grid, inline renaming, and safe deletion)*
- [x] **COLL-03**: Implement bulk upload workflow that groups files into collections upon ingestion. *(Completed: Built multi-file batch upload dialog in `AssetSidebar.tsx` and inline dropzone in `CollectionDetailView.tsx`)*

### Phase E: Archive & Governance
- [x] **GOV-01**: Implement "Archive" soft deletion state for assets. *(Found `archiveAsset` in `AssetService.ts`)*
- [x] **GOV-02**: Add usage protection to prevent archiving assets currently in use. *(Completed: Added pre-flight active usage check throwing AssetInUseError in AssetService.archiveAsset, disabled UI button and added destructive toast notification in AssetInspector)*
- [x] **GOV-03**: Implement asset restoration flow (un-archive). *(Found `restoreAsset` in `AssetService.ts`)*

### Phase F: Search & Intelligence
- [x] **INT-01**: Implement faceted search by Tag, Role, Collection, and Domain. *(Completed: Direct database pushdown for domain/role/tags pre-filtering, collection filtering, date bounds, multi-field search, pagination, and single asset lookup by ID in `AssetService`)*
- [x] **INT-02**: Build intelligence dashboards for Unused Assets, Duplicate Assets, Recent Uploads, Failed Uploads. *(Found `insightFilter` in `AssetSidebar` with unused/failed/recent/duplicates)*

## Traceability
*To be filled by ROADMAP.md*
